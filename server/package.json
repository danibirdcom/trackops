{
  "name": "trackops-sync-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Minimal sync server for TrackOps (REST + SSE, file-backed). Zero external dependencies.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "engines": {
    "node": ">=18"
  }
}
